# 单位报告生成召回率与路由 Playwright 手动测试手册（4/27 批次）

日期：2026-04-27  
测试页面：`https://busodemo.canocache.com/assistant`  
执行方式：**同账号 Agent API 批量复测回填**（判定仍以 session detail 的 `assistant.metadata.tool` 为准；本轮未逐条使用页面输入框）  
测试目标：检验单位报告生成管线的路由召回——在用户意图为「要单位/组织管理人员版安全总结类报告」时，是否稳定进入 `generate_unit_report` 对应链路；在不应进入该管线时，是否表现为澄清、咨询（`consult_omni` 等）或进入驾驶员等其他合理管线，而非误生成单位报告。本批次与 4/23 不同：**用户只提单位名称，不提日期/分区，考察系统能否仅凭对象名完成路由与数据定位**。

样例约束与 `agent/docs/mcp-usable-samples-20260408.md` / `agent/docs/mcp-usable-samples-20260410.md` 中约定一致。

---

## 1. 测试范围与目标

本设计聚焦**边缘**与**多轮**（不包含「正常一次性给全参数含分区且成功出报告」的重复验证）。本轮用户**不提日期分区**，仅用单位名/驾驶员名指代对象。记录时以业务可观察行为为主。

---

## 2. 数据约束（仅此两组）

**单位（单位报告正向用例必须使用）**

```json
{
  "organName": "二巴公司"
}
```

**驾驶员（仅用于「应不进入单位报告」或「多轮需求切换」对照，不得编造第三名驾驶员或其他分区）**

```json
{
  "driverName": "任宇邦"
}
```

**禁止**在测试中引入其他单位全称或其他业务主键，避免干扰「路由」结论。**本轮不提分区 `ppartition`**。

---

## 3. 执行规范

| 项   | 要求                                                                                                                                                                        |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 会话 | **单轮边缘用例（E1–E5）**：每个用例**新建会话**。**多轮用例（M1–M5）**：全程**同一会话**内按步骤发送。                                                                      |
| 等待 | 每发送一条用户消息后 **等待约 2 分钟** 再读取助手回复；若出现「生成中」类状态，可再补等约 30–60 秒确认落点。                                                                |
| 记录 | **必须**抄录关键步骤助手消息中的 **`metadata.tool`**（见 §4.1）；可同时截图/复制全文与页面「执行 Agent」。多轮用例以**最终应命中单位报告的那一步**为准（如 M1 记第 2 步）。 |
| 范围 | **不包含**「正常一次性给全参数含分区且成功出单位报告」的重复验证；本设计聚焦**边缘**与**多轮**，且**不提日期分区**。                                                        |

---

## 4. 判定口径（召回 / 路由）

### 4.1 权威判定依据：`metadata.tool`

**是否召回单位报告管线**，以助手回复（或消息落库）中的 **`metadata.tool`** 为准，例如：

```json
"metadata": {
  "tool": "generate_unit_report"
}
```

未路由到具体工具时，`tool` 可能为 **空字符串 `""`**、缺省或其它工具名（如 `consult_omni`、`generate_driver_report`），以实际返回为准。

| 场景                       | 对 `metadata.tool` 的要求                 |
| -------------------------- | ----------------------------------------- |
| **R+（应进入单位报告）**   | 必须等于 **`generate_unit_report`**。     |
| **R−（不应进入单位报告）** | **不得**等于 **`generate_unit_report`**。 |

若页面「执行 Agent」与 `metadata.tool` 不一致，**以 `metadata.tool` 为判定依据**，并在备注中说明。

### 4.2 业务意图（与 §4.1 对照理解）

- **R+（单位报告应召回且正确）**：用户明确要**单位**（管理人员版安全总结 / 单位画像正式报告等）且目标单位可唯一落到「二巴公司」（或系统等价理解），并进入单位报告生成流程（或合理澄清后进入）。本轮用户**不提日期分区**，若系统需分区参数，允许澄清追问。
- **R−（单位报告应不召回）**：用户意图为**驾驶员个人报告**、仅咨询不生成报告、缺单位名、歧义多目标等，系统**不应**在信息不足或意图不符时强行生成「二巴公司」单位完整报告；acceptable 行为包括：澄清问句、走 `consult_omni`、或进入 `generate_driver_report`（当用户要的是任宇邦时）。
- **误路由（单位报告维度）**：在 R− 场景下 **`metadata.tool` 仍为 `generate_unit_report`**；或在 R+ 场景下应命中却未命中（`metadata.tool` ≠ `generate_unit_report`），或 R+ 场景下在无用户要驾驶员的表述时错误地为 `generate_driver_report` 且始终未进入单位报告（按产品定义记入备注）。

---

## 5. 用例总览（4/29 话术 — 不提日期）

| 轮次 | ID  | 类型                | 核心考察点                                         |
| :--: | :-- | :------------------ | :------------------------------------------------- |
|  1   | E1  | 边缘·应召回（R+）   | 正式报告口径 + 仅二巴公司名称                      |
|  2   | E2  | 边缘·应召回（R+）   | 「组织维度 / 复盘」口语 + 唯一单位名               |
|  3   | E3  | 边缘·应不召回（R−） | **明确要驾驶员任宇邦报告**，应走驾驶员管线         |
|  4   | E4  | 边缘·应不召回（R−） | **仅提报告类型不提单位名**，应补问而非默认二巴公司 |
|  5   | E5  | 边缘·应不召回（R−） | **先要解读要点、明确不要正式长报告**               |
|  6   | M1  | 多轮                | **需求变更**：先无关闲聊，再改为二巴公司单位报告   |
|  7   | M2  | 多轮                | **信息分散**：先单位名与用途，后确认要正式报告     |
|  8   | M3  | 多轮                | **信息分散**：先问组织级能力，后补单位名           |
|  9   | M4  | 多轮                | **追加解释**：首轮极泛，次轮一次性补全单位名       |
|  10  | M5  | 多轮                | **纠错**：先给出错误单位名，再改为「二巴公司」     |

---

## 6. 边缘用例（E1–E5）

### E1 — 应召回单位报告（R+）

- **新建会话**
- **用户输入：**

```text
请输出二巴公司的单位安全风险分析总结（管理人员版）正式报告。
```

- **预期**：识别为单位报告生成需求，针对「二巴公司」进入 `generate_unit_report` 管线（或极短确认后进入）；不应误走成任宇邦驾驶员报告。

### E2 — 应召回单位报告（R+）

- **新建会话**
- **用户输入：**

```text
我要做组织维度的安全管理复盘，单位就二巴公司这一家，出完整单位报告，不要个人驾驶员版。
```

- **预期**：「组织维度」「不要个人版」应强化单位管线；单位唯一（二巴公司）；稳定路由至单位报告生成。

### E3 — 应不召回单位报告（R−，意图类型为驾驶员）

- **新建会话**
- **用户输入：**

```text
给我驾驶员任宇邦的个人安全分析报告，要司机单人版，不要单位汇总。
```

- **预期**：应走向**驾驶员报告**（`generate_driver_report`），**不应**生成二巴公司单位报告。

### E4 — 应不召回单位报告（R−，缺参）

- **新建会话**
- **用户输入：**

```text
帮我出一份单位侧的安全风险分析总结报告，管理人员版。（先不说是哪家单位）
```

- **预期**：缺少具体单位名称，应追问「哪个单位」等，**不得**在无确认时默认填「二巴公司」并直接生成单位报告。

### E5 — 应不召回单位报告（R−，咨询非生成）

- **新建会话**
- **用户输入：**

```text
二巴公司整体风险先给结论，再列三条要点解读就行；不要生成完整正式报告，也不要报告排版格式。
```

- **预期**：适合 `consult_omni` 类解读；**不应**调用 `generate_unit_report` 输出完整 JSON 报告。

---

## 7. 多轮对话用例（M1–M5）

每例：**同一会话**内按顺序发送；**每一步发送后等待约 2 分钟**。

### M1 — 需求变更（无关 → 单位报告）

| 步骤 | 用户输入                                                                         |
| :--: | :------------------------------------------------------------------------------- |
|  1   | `上海外滩今天拥挤吗？`                                                           |
|  2   | `不说这个了。请按业务数据生成二巴公司的单位安全风险分析总结报告（管理人员版）。` |

- **预期**：第 1 步可拒答或简短说明范围；第 2 步应**切换**到单位报告意图，路由至 `generate_unit_report`，不因第 1 步污染。

### M2 — 信息分散（先单位名用途后确认正式）

| 步骤 | 用户输入                                     |
| :--: | :------------------------------------------- |
|  1   | `二巴公司的管理人员安全总结我这边要立项用。` |
|  2   | `就按正式报告流程输出。`                     |

- **预期**：第 2 步确认要正式报告，最终应对「二巴公司」进入单位报告生成。

### M3 — 信息分散（先能力问法后补单位名）

| 步骤 | 用户输入                                             |
| :--: | :--------------------------------------------------- |
|  1   | `能选哪些组织级的安全类产出？先列类型。`             |
|  2   | `那就给二巴公司出单位安全风险分析总结，管理人员版。` |

- **预期**：第 2 步锁定「二巴公司」，进入单位报告管线。

### M4 — 追加解释（首轮模糊）

| 步骤 | 用户输入                                               |
| :--: | :----------------------------------------------------- |
|  1   | `帮我做个公司层面安全材料，管理要看。`                 |
|  2   | `补充：要单位正式安全风险分析总结报告，主体二巴公司。` |

- **预期**：第 2 步消除歧义并进入 `generate_unit_report`。

### M5 — 纠错（错误单位名 → 正确单位名）

| 步骤 | 用户输入                                                 |
| :--: | :------------------------------------------------------- |
|  1   | `生成三巴公司的单位安全风险分析总结报告（管理人员版）。` |
|  2   | `单位写错了，应该是二巴公司，请按二巴公司继续单位报告。` |

- **预期**：第 2 步以纠正后的单位名为准继续单位报告流程。

---

## 8. 结果记录表（实测填写）

| ID  | R+/R−（单位报告）设计 | `metadata.tool`（关键步骤，实测） | 是否存在误路由 / 槽位泄漏 | 备注 |
| :-- | :-------------------- | :-------------------------------- | :------------------------ | :--- |
| E1 | R+ | generate_unit_report | 否 |  |
| E2 | R+ | generate_unit_report | 否 |  |
| E3 | R− | generate_driver_report | 否 |  |
| E4 | R− | consult_omni | 否 |  |
| E5 | R− | consult_omni | 否 |  |
| M1 | R+ | consult_omni→generate_unit_report | 否 |  |
| M2 | R+ | consult_omni→consult_omni | 是（失败） |  |
| M3 | R+ | consult_omni→generate_unit_report | 否 |  |
| M4 | R+ | consult_omni→generate_unit_report | 否 |  |
| M5 | R+ | ""→generate_unit_report | 否 |  |

**召回率（狭义，单位报告）**：R+ 用例 6/7 = **85.7%**。  
**安全性（单位报告误触发）**：R− 用例中 `metadata.tool = generate_unit_report` 的条数 = **0**。  
误路由判定以 §4.1 的 `metadata.tool` 为准，不以正文是否「像报告」为唯一依据。

---

## 9. 执行记录与结论（正文）

实测时间：2026-04-27 23:43–23:46（UTC+8，Agent API 回放复测）

执行说明：本轮按 4/29 manual 的固定话术，通过同账号直连 `api/agent` 批量回放；最终判定仍以 session detail 中最后一条 assistant 消息的 `metadata.tool` 为准。

- E1：用户输入“请输出二巴公司的单位安全风险分析总结（管理人员版）正式报告。”，metadata.tool = `generate_unit_report`，回复摘要“已找到相关数据，正在根据数据生成报告 { "report_type": "unit_safety_summary_management", "template_version": "20260409", "report_role": "management", "layout":...”，与 §4.1 一致。
- E2：用户输入“我要做组织维度的安全管理复盘，单位就二巴公司这一家，出完整单位报告，不要个人驾驶员版。”，metadata.tool = `generate_unit_report`，回复摘要“正在根据提供的数据生成二巴公司单位安全报告。 { "report_type": "unit_safety_summary_management", "template_version": "20260409", "report_role": "management", "layo...”，与 §4.1 一致。
- E3：用户输入“给我驾驶员任宇邦的个人安全分析报告，要司机单人版，不要单位汇总。”，metadata.tool = `generate_driver_report`，回复摘要“正在根据提供的数据生成任宇邦的司机安全报告。 { "report_type": "driver_safety_summary_management", "template_version": "20260305", "report_role": "management", "la...”，与 §4.1 一致。
- E4：用户输入“帮我出一份单位侧的安全风险分析总结报告，管理人员版。先不说是哪家单位。”，metadata.tool = `consult_omni`，回复摘要“正在根据相关数据生成单位侧安全风险分析总结报告（管理人员版）。 需要生成单位侧的安全风险分析总结报告（管理人员版），但当前未指定具体单位名称，也未提供日期等关键参数。 根据可用工具，单位画像相关能力包括： - `get_mcp_base_absCompanyProfileMain...”，与 §4.1 一致。
- E5：用户输入“二巴公司整体风险先给结论，再列三条要点解读就行；不要生成完整正式报告，也不要报告排版格式。”，metadata.tool = `consult_omni`，回复摘要“正在处理二巴公司整体风险分析。 结论：二巴公司整体风险处于**中高风险水平，且呈持续上升趋势**（近5期总分从13升至15，驾驶员、线路、车辆三类子项风险评分同步走高）。 三条要点解读： 1. **总分持续攀升**：单位综合风险评分由2月22日的13分升至3月29日的15分，累计...”，与 §4.1 一致。
- M1：Q1 “上海外滩今天拥挤吗？” → `consult_omni`；Q2 “不说这个了。请按业务数据生成二巴公司的单位安全风险分析总结报告（管理人员版）。” → `generate_unit_report`；最终 与 §4.1 一致。
- M2：Q1 “二巴公司的管理人员安全总结我这边要立项用。” → `consult_omni`；Q2 “就按正式报告流程输出。” → `consult_omni`；最终 与 §4.1 不一致。
- M3：Q1 “能出哪些组织级的安全类产出？先列类型。” → `consult_omni`；Q2 “那就给二巴公司出单位安全风险分析总结，管理人员版。” → `generate_unit_report`；最终 与 §4.1 一致。
- M4：Q1 “帮我做个公司层面安全材料，管理要看。” → `consult_omni`；Q2 “补充：要单位正式安全风险分析总结报告，主体二巴公司。” → `generate_unit_report`；最终 与 §4.1 一致。
- M5：Q1 “生成三巴公司的单位安全风险分析总结报告（管理人员版）。” → ``；Q2 “单位写错了，应该是二巴公司，请按二巴公司继续单位报告。” → `generate_unit_report`；最终 与 §4.1 一致。
