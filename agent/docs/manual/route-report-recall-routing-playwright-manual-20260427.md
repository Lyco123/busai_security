# 线路报告生成召回率与路由 Playwright 手动测试手册（4/27 批次）

日期：2026-04-27  
测试页面：`https://busodemo.canocache.com/assistant`  
执行方式：**同账号 Agent API 批量复测回填**（判定仍以 session detail 的 `assistant.metadata.tool` 为准；本轮未逐条使用页面输入框）  
测试目标：专项检验线路报告生成管线的**召回率**（应进入线路报告生成相关流程）、**不误召**（不应进入该管线）、**澄清行为**是否合理、以及长等待下的**系统稳定性**。本批次与 4/23 不同：**用户只提线路名称，不提日期/分区，考察系统能否仅凭对象名完成路由与数据定位**。

样例数据来源：`agent/docs/mcp-usable-samples-20260410.md`（与 demo `fixtures` 一致）。

---

## 1. 测试范围（本轮不覆盖）

- 标准、规范的「请生成线路安全风险分析总结报告 + 线路名 + 分区」等**正常表述含分区**已在其他轮次验证，**本轮不测**。
- 本轮聚焦：**仅用线路名（不提日期分区）/ 近义词 / 口语 / 中英混排 / 参数分散与澄清 / 易混意图**。

---

## 2. 数据约束（唯一允许）

仅允许使用以下线路数据（勿改线路名）：

```json
{ "routeName": "527" }
```

与车辆样例交叉对照时，仅可使用同文档中的车牌（用于**不应进入线路报告**的负例）：`粤A02650D`、`粤A02013D`。

**本轮不提分区 `ppartition`**，用户输入中仅用线路名指代对象。

---

## 3. 执行规范

| 项   | 要求                                                                                                                                            |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 会话 | **每一轮（R+1～R−5）单独新建会话**，避免上文干扰路由判定。                                                                                      |
| 等待 | 每发送**一条**用户消息后，**等待约 2 分钟**再读取助手回复（大模型较慢）；若界面出现「生成中」等中间态，可在最终文案出现后再补等数十秒确认落点。 |
| 记录 | **必须**抄录助手消息中的 **`metadata.tool`**（见 §4.1）；可同时记录页面「执行 Agent」、错误页/超时/重试。                                       |

---

## 4. 判定口径

### 4.1 权威判定依据：`metadata.tool`

**是否召回本管线**，以助手回复（或消息落库）中的 **`metadata.tool`** 为准，例如：

```json
"metadata": {
  "tool": "generate_route_report"
}
```

未路由到具体工具时，`tool` 可能为 **空字符串 `""`**、缺省或其它工具名，以实际返回为准。

| 场景               | 对 `metadata.tool` 的要求                  |
| ------------------ | ------------------------------------------ |
| **应召回（R+）**   | 必须等于 **`generate_route_report`**。     |
| **不应召回（R−）** | **不得**等于 **`generate_route_report`**。 |

若页面「执行 Agent」与 `metadata.tool` 不一致，**以 `metadata.tool` 为判定依据**，并在备注中说明。

### 4.2 业务意图（与 §4.1 对照理解）

- **应召回（R+）**：用户真实意图为「指定线路的正式线路安全/管理向总结类报告」，且能落到 `routeName = 527` 时，应进入线路报告生成管线（含：先合理澄清缺参再进入）。本轮用户**不提日期分区**，系统需仅凭线路名完成路由；若系统需分区参数，允许澄清追问。
- **不应召回（R−）**：意图为**车辆**报告、仅属性查询、明确拒绝报告、多线路对比/泛咨询等，**不得**进入线路报告生成管线；acceptable 为澄清、`consult_omni`、基础查询、或进入 `generate_vehicle_report`（当用户明确要车时）等。

---

## 5. 十轮用例设计

### 5.1 应召回（5 例）— 仅线路名、不提日期

| 轮次 | ID  | 考察点                             | 用户输入（可复制）                                                                                            |
| :--: | :-- | :--------------------------------- | :------------------------------------------------------------------------------------------------------------ |
|  1   | R+1 | 「报告」的近义：调度管理、风险汇总 | **527路**，调度管理要看**安全风险汇总**，管理人员版。                                                         |
|  2   | R+2 | 口语化「搞一份输出」               | **搞一份** **527路** **安全方面**的正式输出，别给碎片化问答。                                                 |
|  3   | R+3 | 「527路」带路字、正式版            | **527路** 的**线路安全总结报告**。                                                                            |
|  4   | R+4 | **澄清**：首轮缺参，次轮补线路名   | **Q1：** 我要**线路级**的安全总结，模块别缺。 **（等待回复后再发 Q2，仍在本会话）** **Q2：** 线路 **527路**。 |
|  5   | R+5 | 中英混排、ops 表述                 | **Ops safety summary (management)** for **route 527**.                                                        |

**R+ 预期摘要：** 最终 `metadata.tool` 应为 **`generate_route_report`**；R+4 允许第一轮仅澄清，第二轮补参后应命中该工具。

---

### 5.2 不应召回（5 例）— 易混意图、否定、多线路、无线路主体

| 轮次 | ID  | 考察点                                    | 用户输入（可复制）                                                          |
| :--: | :-- | :---------------------------------------- | :-------------------------------------------------------------------------- |
|  6   | R−1 | 易混：**车辆**安全报告 vs 线路报告        | 我要**车辆**安全报告，车牌 **粤A02013D**，**别提线路编号**。                |
|  7   | R−2 | 明确不要「报告」，点状运营信息            | **别出正式报告**，只要 **527路** 的**配车数、首末班**这类**运营点点信息**。 |
|  8   | R−3 | 仅确认数据是否存在（非报告）              | **不要生成报告**。请确认 **527路** **有没有线路画像数据**就行。             |
|  9   | R−4 | 两条线路对比/概况（非单线路长篇报告管线） | **527** 和 **100130** **对比概况**，**不要只给一条线的长篇正式报告**。      |
|  10  | R−5 | 无具体线路、泛问题（测不误召与稳定性）    | 公交线路客流高峰安全风险一般怎么谈？**先不说具体哪条线路。**                |

**R− 预期摘要：** `metadata.tool` **不得**为 **`generate_route_report`**；可出现澄清、车辆报告、其他 Agent、或通用问答。

---

## 6. 结果记录表（实测填写）

| 轮次 | ID  | 类型     | 核心考察点   | `metadata.tool` | 是否误路由 | 澄清是否合理 | 稳定性（超时/报错） | 备注 |
| :--: | :-- | :------- | :----------- | :-------------- | :--------: | :----------- | :------------------ | :--- |
|  1 | R+1 | 应召回 | 仅线路名 | generate_route_report | 否 | — | 稳定 |  |
|  2 | R+2 | 应召回 | 口语输出 | generate_route_report | 否 | — | 稳定 |  |
|  3 | R+3 | 应召回 | 带路字正式版 | generate_route_report | 否 | — | 稳定 |  |
|  4 | R+4 | 应召回 | 多轮澄清 | consult_omni→consult_route_expert | 是（失败） | Q1=consult_omni，Q2=consult_route_expert，最终未命中 | 稳定 | 进入 consult_route_expert，未落到目标报告管线 |
|  5 | R+5 | 应召回 | 中英 ops | consult_route_expert | 是（失败） | — | 稳定 | 进入 consult_route_expert，未落到目标报告管线 |
|  6 | R−1 | 不应召回 | 车辆易混 | generate_vehicle_report | 否 | — | 稳定 |  |
|  7 | R−2 | 不应召回 | 点状运营信息 | consult_omni | 否 | — | 稳定 | 正文像报告，但 metadata.tool 仍为 consult_omni |
|  8 | R−3 | 不应召回 | 仅确认有数据 | consult_route_expert | 否 | — | 稳定 |  |
|  9 | R−4 | 不应召回 | 多线对比 | consult_route_expert | 否 | — | 稳定 |  |
| 10 | R−5 | 不应召回 | 无线路泛问 | consult_omni | 否 | — | 稳定 |  |

说明：**「是否误路由」** — 以 §4.1 为准：R+ 若 `metadata.tool` ≠ `generate_route_report` 记失败；R− 若 `metadata.tool` = `generate_route_report` 记失败。

---

## 7. 执行记录（正文）

实测时间：2026-04-27 23:41–23:43（UTC+8，Agent API 回放复测）

执行说明：本轮按 4/29 manual 的固定话术，通过同账号直连 `api/agent` 批量回放；最终判定仍以 session detail 中最后一条 assistant 消息的 `metadata.tool` 为准。

- R+1：用户输入“527路，调度管理要看安全风险汇总，管理人员版。”，metadata.tool = `generate_route_report`，回复摘要“正在根据提供的数据生成527路安全报告。 { "report_type": "route_safety_summary_management", "template_version": "20260305", "report_role": "management", "layou...”，与 §4.1 一致。
- R+2：用户输入“搞一份 527路 安全面的正式输出，别给碎片化问答。”，metadata.tool = `generate_route_report`，回复摘要“正在根据提供的数据生成527路安全报告。 { "report_type": "route_safety_summary_management", "template_version": "20260305", "report_role": "management", "layou...”，与 §4.1 一致。
- R+3：用户输入“527路 的线路安全总结报告。”，metadata.tool = `generate_route_report`，回复摘要“正在根据提供的数据生成527路安全报告。 { "report_type": "route_safety_summary_management", "template_version": "20260305", "report_role": "management", "layou...”，与 §4.1 一致。
- R+4：Q1 “我要线路级的安全总结，模块别缺。” → `consult_omni`；Q2 “线路 527路。” → `consult_route_expert`；最终 与 §4.1 不一致。
- R+5：用户输入“Ops safety summary (management) for route 527.”，metadata.tool = `consult_route_expert`，回复摘要“已找到相关数据，正在根据数据生成报告。 Based on the comprehensive analysis of route 527 here is the operational safety summary for management: **Conclusion:** ...”，与 §4.1 不一致。
- R−1：用户输入“我要车辆安全报告，车牌 粤A02013D，别提线路编号。”，metadata.tool = `generate_vehicle_report`，回复摘要“正在根据提供的数据生成车辆安全报告。 { "report_type": "vehicle_safety_summary_management", "template_version": "20260305", "report_role": "management", "layou...”，与 §4.1 一致。
- R−2：用户输入“别出正式报告，只要527路的配车数、首末班这类运营点点信息。”，metadata.tool = `consult_omni`，回复摘要“已找到相关数据，正在根据数据生成报告。 查询到两条名称含“527”的线路： 1. **527路（主线）** - 配车数量：未提供（`busCount` 字段为空） - 首班车时间：上行方向（广州白云站公交总站 → 石溪总站）为 **05:50** - 末班车时间：下行方向（石溪总...”，与 §4.1 一致。
- R−3：用户输入“不要生成报告。请确认527路有没有线路画像数据就行。”，metadata.tool = `consult_route_expert`，回复摘要“正在查询527路的线路画像数据。 当前缺少527路的线路画像数据。”，与 §4.1 一致。
- R−4：用户输入“527 和 100130 对比概况，不要只给一条线的长篇正式报告。”，metadata.tool = `consult_route_expert`，回复摘要“正在查询527路和100130路的线路信息，准备生成对比概况报告。 要对“527路”和“100130路”进行对比概况分析，需分别获取两条线路的基础画像、风险评分、运行特征（如车辆数、驾驶员数、里程）、黑点路段、近期风险追踪等核心维度数据。 但当前**缺少明确的线路名称/编号对应关...”，与 §4.1 一致。
- R−5：用户输入“公交线路客流高峰安全风险一般怎么谈？先不说具体哪条线路。”，metadata.tool = `consult_omni`，回复摘要“公交线路客流高峰安全风险属于运营组织与驾驶员行为耦合类风险，需从“人、车、路、环、管”五个维度综合分析。以下是典型谈法要点（基于行业通用治理逻辑，非调用工具生成）： ✅ 一、核心风险表现 - 驾驶员压力增大：高峰期连续发车、乘客拥挤上下车干扰、情绪焦躁易引发操作急躁（如急刹、抢行...”，与 §4.1 一致。

召回率（线路管线）：R+ 用例 3/5 = **60%**
安全性（线路管线误触发）：R− 用例 0/5 误触发 = **100% 正确**
