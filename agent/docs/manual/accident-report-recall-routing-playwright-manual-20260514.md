# 事故调查（整改）报告生成召回率与路由 Playwright 手动测试手册（5/14 批次）

日期：2026-05-14  
测试页面：`https://busodemo.canocache.com/assistant`  
执行方式：**Playwright 手动**逐条发送用户消息  
测试目标：专项检验**事故调查 / 整改 / 复盘类正式报告**管线的**召回率**（应进入 `generate_accident_investigation_report`）、**不误召**（查询台账、基础信息、统计等不应走报告生成）、**澄清行为**是否合理、以及长等待下的**系统稳定性**。本批次为 5/14 全管线回归批次：**用户只提事故标识或别名，不提日期/分区，考察系统能否仅凭对象名完成路由与数据定位**。


---

## 1. 测试范围（本轮不覆盖）

- 标准、规范的「请生成事故调查报告 + 事故编号 + 分区」等**一次性给全参数含分区**表述可在其他轮次验证，**本轮不测**。
- 本轮聚焦：**仅用事故标识/别名指代（不提日期分区）/ 口语 / 中英混排 / 多轮补参（只补标识不补日期） / 与「只查事故信息不要报告」的边界**。

---

## 2. 数据约束（唯一允许）

仅允许使用以下事故标识（与 fixtures 一致，勿改编号；自然语言可等价指向同一事故）：

```json
{ "incidentId": "INC-20251014-01" }
```

**可选用作口语指代的别名（与 local 一致，勿编造新事故）：**

- `环市西路段刮碰事故`（本轮**不**附带日期前缀，仅用描述性别名）
- `363路`（用于考察歧义风险下的澄清；若环境允许多匹配则需记录实际行为）

与驾驶员报告交叉对照时，仅可使用同仓库样例中的驾驶员（用于**不应进入事故报告生成**的负例）：`任宇邦`（`03001982`）（**不要**与事故 fixtures 混用为同一意图槽位，负例中明确只要驾驶员报告即可）。

---

## 3. 执行规范

| 项   | 要求                                                                                                                                            |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 会话 | **每一轮（R+1～R−5）单独新建会话**，避免上文干扰路由判定。                                                                                      |
| 等待 | 每发送**一条**用户消息后，**等待约 2 分钟**再读取助手回复（大模型较慢）；若界面出现「生成中」等中间态，可在最终文案出现后再补等数十秒确认落点。 |
| 记录 | **必须**记录该轮助手消息中的 **`metadata.tool`**（见第 4 节）；建议同时记录页面展示的「执行 Agent」、是否报错/超时。                            |

---

## 4. 判定口径

### 4.1 权威判定依据：`metadata.tool`

**是否召回本管线**，以助手回复（或消息落库）中的 **`metadata.tool`** 字符串为准，例如：

```json
"metadata": {
  "tool": "generate_accident_investigation_report"
}
```

未路由到具体工具时，`tool` 可能为 **空字符串 `""`**、缺省、`consult_omni`、专家咨询类工具名等，以实际返回为准。

| 场景               | 对 `metadata.tool` 的要求                                                                               |
| ------------------ | ------------------------------------------------------------------------------------------------------- |
| **应召回（R+）**   | 必须等于 **`generate_accident_investigation_report`**（事故调查/整改类报告管线对应的 Agent / 工具名）。 |
| **不应召回（R−）** | **不得**等于 **`generate_accident_investigation_report`**。                                             |

若 UI 展示与 `metadata.tool` 不一致，**以 `metadata.tool` 为判定依据**，并在备注中写明 UI 表现。

### 4.2 业务意图（与 `metadata.tool` 对照）

- **应召回（R+）**：用户明确要求生成**事故调查报告、整改报告、事故复盘报告**等**正式结构化报告**，且能落到 `INC-20251014-01` 或其允许的别名指代（含合理澄清后补全标识）。本轮用户**不提日期分区**，系统需仅凭事故标识完成路由。
- **不应召回（R−）**：用户只是在**查询事故信息、基础信息、详情、台账、列表、统计、处理进度**等，或明确**不要生成完整正式报告**、或意图为**驾驶员等其它主体报告**时，**不得**进入 `generate_accident_investigation_report`。

---

## 5. 十轮用例设计

### 5.1 应召回（5 例）

| 轮次 | ID  | 考察点                         | 用户输入（可复制）                                                                                                                  |
| :--: | :-- | :----------------------------- | :---------------------------------------------------------------------------------------------------------------------------------- |
|  1   | R+1 | 仅事故编号、不提日期           | 事故 **INC-20251014-01**，帮出一份**事故调查与整改措施**正式报告，管理口径，章节齐全。                                              |
|  2   | R+2 | 口语化别名、不提日期           | **搞一份** **环市西路段刮碰事故** 的**事故调查复盘**，要按正式调查报告格式输出，别给零碎问答。                                      |
|  3   | R+3 | 编号 + 事故简称、不提日期      | **INC-20251014-01** 那起刮碰事故，我要**事故调查报告（含整改）**，标识对不上再问我。                                                |
|  4   | R+4 | **澄清**：首轮缺标识，次轮补全 | **Q1：** 帮我生成事故调查整改的正式报告，格式要齐。 **（等待回复后再发 Q2，仍在本会话）** **Q2：** 事故编号 **INC-20251014-01**。   |
|  5   | R+5 | 中英混排、不提日期             | Please **generate the accident investigation and remediation report** for incident **INC-20251014-01** (formal management version). |

**R+ 预期摘要：** 最终 `metadata.tool` 应为 **`generate_accident_investigation_report`**；R+4 允许首轮未命中工具，Q2 补全后应命中。

---

### 5.2 不应召回（5 例）

| 轮次 | ID  | 考察点                              | 用户输入（可复制）                                                                        |
| :--: | :-- | :---------------------------------- | :---------------------------------------------------------------------------------------- |
|  6   | R−1 | 只查详情 / 台账感，不要生成正式报告 | **不要出调查报告**。把 **INC-20251014-01** 的**事故经过与责任认定**查出来即可，短文就行。 |
|  7   | R−2 | 统计 / 列表感                       | **环市西路段刮碰事故** 同类事故还有几条？列编号和标题就行，**不要**给我整篇整改报告。     |
|  8   | R−3 | 明确否定「完整正式报告」            | **INC-20251014-01** 先**解读三条风险要点**就行，**不要**输出完整正式调查报告排版。        |
|  9   | R−4 | 易混：**驾驶员**个人安全报告        | 我要 **任宇邦** 的**驾驶员安全分析报告**，个人版；**别提事故编号**。                      |
|  10  | R−5 | 泛问法规流程                        | 公交事故内部调查与整改的一般流程是怎样的？**不提具体事故编号。**                          |

**R− 预期摘要：** `metadata.tool` **不得**为 **`generate_accident_investigation_report`**。

---

## 6. 结果记录表（实测填写）

| 轮次 | ID  | 类型     | 核心考察点       | `metadata.tool`（实测）                        | 是否误路由 | 澄清是否合理                   | 稳定性 | 备注                            |
| :--: | :-- | :------- | :--------------- | :--------------------------------------------- | :--------: | :----------------------------- | :----- | :------------------------------ |
|  1   | R+1 | 应召回   | 仅编号不提日期   | `generate_accident_investigation_report`       |     否     | —                              | 正常   | ✅ 命中                         |
|  2   | R+2 | 应召回   | 口语别名不提日期 | null（upstream_error，别名查询失败）           |     是     | —                              | 异常   | ❌ 失败：别名未路由到事故管线   |
|  3   | R+3 | 应召回   | 编号+简称        | `generate_accident_investigation_report`       |     否     | —                              | 正常   | ✅ 命中                         |
|  4   | R+4 | 应召回   | 多轮澄清         | `generate_accident_investigation_report`（Q2） |     否     | 合理（Q1追问编号，Q2补参命中） | 正常   | ✅ 命中（Q2补参后命中）         |
|  5   | R+5 | 应召回   | 中英不提日期     | `generate_accident_investigation_report`       |     否     | —                              | 正常   | ✅ 命中                         |
|  6   | R−1 | 不应召回 | 查详情           | `consult_incident_expert`                      |     否     | —                              | 正常   | ✅ 正确拒绝（咨询而非生成报告） |
|  7   | R−2 | 不应召回 | 列表统计         | `consult_omni`                                 |     否     | —                              | 正常   | ✅ 正确拒绝（通用咨询而非报告） |
|  8   | R−3 | 不应召回 | 不要正式报告     | `consult_incident_expert`                      |     否     | —                              | 正常   | ✅ 正确拒绝（咨询而非生成报告） |
|  9   | R−4 | 不应召回 | 驾驶员报告       | `generate_driver_report`                       |     否     | —                              | 正常   | ✅ 正确拒绝（走向驾驶员管线）   |
|  10  | R−5 | 不应召回 | 泛问             | `consult_omni`                                 |     否     | —                              | 正常   | ✅ 正确拒绝（通用咨询而非报告） |

说明：**「是否误路由」** — 对 R+：`metadata.tool` ≠ `generate_accident_investigation_report` 记为失败；对 R−：`metadata.tool` = `generate_accident_investigation_report` 记为失败。

---

## 7. 执行记录（正文）

### R+1 — 仅编号不提日期（INC-20251014-01 事故调查与整改报告）

- 用户原文：事故 INC-20251014-01，帮出一份事故调查与整改措施正式报告，管理口径，章节齐全。
- `metadata.tool`：`generate_accident_investigation_report`
- 与 §4.1 一致：**是**（应召回且命中）

### R+2 — 口语别名不提日期（环市西路段刮碰事故）

- 用户原文：搞一份 环市西路段刮碰事故 的事故调查复盘，要按正式调查报告格式输出，别给零碎问答。
- `metadata.tool`：null（upstream_error，别名查询失败）
- 与 §4.1 一致：**否**（应命中 `generate_accident_investigation_report` 但别名未能路由，❌ 失败）

### R+3 — 编号+简称（INC-20251014-01 那起刮碰事故）

- 用户原文：INC-20251014-01 那起刮碰事故，我要事故调查报告（含整改），标识对不上再问我。
- `metadata.tool`：`generate_accident_investigation_report`
- 与 §4.1 一致：**是**（应召回且命中）

### R+4 — 多轮澄清（Q1缺编号→Q2补全）

- Q1 用户原文：帮我生成事故调查整改的正式报告，格式要齐。
- Q1 `metadata.tool`：`request_further_info`（追问事故编号）
- Q2 用户原文：事故编号 INC-20251014-01。
- Q2 `metadata.tool`：`generate_accident_investigation_report`
- 与 §4.1 一致：**是**（多轮澄清后命中）

### R+5 — 中英混排（INC-20251014-01 formal management version）

- 用户原文：Please generate the accident investigation and remediation report for incident INC-20251014-01 (formal management version).
- `metadata.tool`：`generate_accident_investigation_report`
- 与 §4.1 一致：**是**（应召回且命中）

### R−1 — 查详情（不要出调查报告，只查事故经过与责任认定）

- 用户原文：不要出调查报告。把 INC-20251014-01 的事故经过与责任认定查出来即可，短文就行。
- `metadata.tool`：`consult_incident_expert`
- 与 §4.1 一致：**是**（不应召回，✅ 正确拒绝）

### R−2 — 列表统计（同类事故编号和标题，不要整改报告）

- 用户原文：环市西路段刮碰事故 同类事故还有几条？列编号和标题就行，不要给我整篇整改报告。
- `metadata.tool`：`consult_omni`
- 与 §4.1 一致：**是**（不应召回，✅ 正确拒绝）

### R−3 — 不要正式报告（解读三条风险要点就行）

- 用户原文：INC-20251014-01 先解读三条风险要点就行，不要输出完整正式调查报告排版。
- `metadata.tool`：`consult_incident_expert`
- 与 §4.1 一致：**是**（不应召回，✅ 正确拒绝）

### R−4 — 驾驶员报告（任宇邦驾驶员安全分析报告，别提事故编号）

- 用户原文：我要 任宇邦 的驾驶员安全分析报告，个人版；别提事故编号。
- `metadata.tool`：`generate_driver_report`
- 与 §4.1 一致：**是**（不应召回事故管线，走向驾驶员管线，✅ 正确拒绝）

### R−5 — 泛问（公交事故调查整改一般流程，不提编号）

- 用户原文：公交事故内部调查与整改的一般流程是怎样的？不提具体事故编号。
- `metadata.tool`：`consult_omni`
- 与 §4.1 一致：**是**（不应召回，✅ 正确拒绝）

---

## 召回率统计

- **R+ 命中率**：`generate_accident_investigation_report` 命中数 ÷ R+ 总数 = 4/5 = **80%**（R+1✅、R+2❌别名查询失败、R+3✅、R+4✅、R+5✅）
- **R− 正确率**：非 `generate_accident_investigation_report` 数 ÷ R− 总数 = 5/5 = **100%**（R−1✅、R−2✅、R−3✅、R−4✅、R−5✅）
- **误触发数**：0（R− 用例中无 `generate_accident_investigation_report`）
