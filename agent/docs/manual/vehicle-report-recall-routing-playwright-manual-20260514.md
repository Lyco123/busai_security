# 车辆报告生成召回率与路由 Playwright 手动测试手册（5/14 批次）

日期：2026-05-14  
测试页面：`https://busodemo.canocache.com/assistant`  
执行方式：**Playwright 手动**逐条发送用户消息  
测试目标：专项复检车辆报告生成管线的**召回率**（应进入 `generate_vehicle_report` 相关流程）、**不误召**（不应进入该管线）、**澄清行为**是否合理、以及长等待下的**系统稳定性**。本批次为 5/14 全管线回归批次：**用户只提车牌号，不提日期/分区，考察系统能否仅凭对象名完成路由与数据定位**。

样例数据来源：`agent/docs/mcp-usable-samples-20260410.md`（与 demo `fixtures` 一致）。

---

## 1. 测试范围（本轮不覆盖）

- 标准、规范的「请生成车辆安全风险分析总结报告 + 完整车牌 + 分区」等**正常表述含分区**已在其他轮次验证，**本轮不测**。
- 本轮聚焦：**仅用车牌号（不提日期分区）/ 近义词 / 口语 / 错别字与不规范写法 / 中英混排 / 参数分散与澄清 / 易混意图**。

---

## 2. 数据约束（唯一允许）

仅允许使用以下两组真实数据（勿改车牌号）：

```json
{ "numberplate": "粤A02650D" }
```

```json
{ "numberplate": "粤A02013D" }
```

与驾驶员样例交叉对照时，仅可使用同文档中的驾驶员姓名（用于**不应进入车辆报告**的负例）：`任宇邦`（工号`03001982`）、`关飞鸿`（工号`03001402`）。

**本轮不提分区 `ppartition`**，用户输入中仅用车牌号指代对象。

---

## 3. 执行规范

| 项   | 要求                                                                                                                                            |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 会话 | **每一轮（R+1～R−5）单独新建会话**，避免上文干扰路由判定。                                                                                      |
| 等待 | 每发送**一条**用户消息后，**等待约 2 分钟**再读取助手回复（大模型较慢）；若界面出现「生成中」等中间态，可在最终文案出现后再补等数十秒确认落点。 |
| 记录 | **必须**抄录助手消息中的 **`metadata.tool`**（见 §4.1）；可同时记录页面「执行 Agent」、错误页/超时/重试。                                       |

---

## 4. 判定口径

### 4.1 权威判定依据：`metadata.tool`

**是否召回本管线**，以助手回复（或消息落库）中的 **`metadata.tool`** 为准，例如：

```json
"metadata": {
  "tool": "generate_vehicle_report"
}
```

未路由到具体工具时，`tool` 可能为 **空字符串 `""`**、缺省或其它工具名，以实际返回为准。

| 场景               | 对 `metadata.tool` 的要求                    |
| ------------------ | -------------------------------------------- |
| **应召回（R+）**   | 必须等于 **`generate_vehicle_report`**。     |
| **不应召回（R−）** | **不得**等于 **`generate_vehicle_report`**。 |

若页面「执行 Agent」与 `metadata.tool` 不一致，**以 `metadata.tool` 为判定依据**，并在备注中说明。

### 4.2 业务意图（与 §4.1 对照理解）

- **应召回（R+）**：用户真实意图为「指定车辆的正式车辆安全/管理向总结类报告」，且能落到上述两组 `(numberplate)` 之一时，应进入车辆报告生成管线（含：先合理澄清缺参再进入）。本轮用户**不提日期分区**，系统需仅凭车牌号完成路由；若系统需分区参数，允许澄清追问。
- **不应召回（R−）**：意图为**驾驶员**报告、**线路**报告、仅档案/属性查询、明确拒绝报告、多车对比/泛咨询等，**不得**进入车辆报告生成管线；acceptable 为澄清、`consult_omni`、基础查询、或进入 `generate_driver_report` / 线路相关能力（当用户明确要人/线时）等。

---

## 5. 十轮用例设计

### 5.1 应召回（5 例）— 仅车牌号、不提日期

| 轮次 | ID  | 考察点                         | 用户输入（可复制）                                                                                                      |
| :--: | :-- | :----------------------------- | :---------------------------------------------------------------------------------------------------------------------- |
|  1   | R+1 | 「报告」的近义：车辆风险画像   | 车牌 **粤A02013D**，管理层要看**车辆安全风险画像**，按管理视角整一份完整输出。                                          |
|  2   | R+2 | 口语化「帮搞一份」             | **帮搞一份** **粤A02650D** 的**安全方面整车总结**，不要碎片化问答那种。                                                 |
|  3   | R+3 | 不规范：粤字写成广东           | 车牌 **广东A02650D**（**广东**即**粤**），要**车辆安全总结报告**，正式版。                                              |
|  4   | R+4 | **澄清**：首轮缺车牌，次轮补全 | **Q1：** 我要**车辆层面的安全总结**，模块给全。 **（等待回复后再发 Q2，仍在本会话）** **Q2：** 车牌号是 **粤A02650D**。 |
|  5   | R+5 | 中英混排、vehicle 表述         | **Vehicle safety risk profile** (management version) for plate **粤A02013D**; full formal output.                       |

**R+ 预期摘要：** 最终 `metadata.tool` 应为 **`generate_vehicle_report`**；R+4 允许第一轮仅澄清，第二轮补参后应命中该工具。

---

### 5.2 不应召回（5 例）— 易混意图、否定、多车、无车牌

| 轮次 | ID  | 考察点                                | 用户输入（可复制）                                                                |
| :--: | :-- | :------------------------------------ | :-------------------------------------------------------------------------------- |
|  6   | R−1 | 易混：**驾驶员**个人版 vs 车辆报告    | 出一份**驾驶员**安全画像，师傅 **任宇邦**，**不要车辆维度**。                     |
|  7   | R−2 | 明确不要「正式报告」，只要零碎信息    | **不用出长篇报告**，查一下 **粤A02650D** 的**所属车队、年检到期日**就行。         |
|  8   | R−3 | 仅确认数据存在（非报告）              | **别生成报告**。告诉我 **粤A02013D** **有没有车辆安全画像数据**就好。             |
|  9   | R−4 | 两车 + 并排概况（非单车长篇报告管线） | **粤A02650D** 和 **粤A02013D** 两台车**一起看安全概况**，**不给单车的长篇报告**。 |
|  10  | R−5 | 无车牌、泛问题（测不误召与稳定性）    | 公交车辆日常安全管理一般需要关注哪些方面？**不提具体车牌号。**                    |

**R− 预期摘要：** `metadata.tool` **不得**为 **`generate_vehicle_report`**；可出现澄清、驾驶员/线路报告、其他 Agent、或通用问答。

---

## 6. 结果记录表（实测填写）

| 轮次 | ID  | 类型     | 核心考察点     | `metadata.tool`           | 是否误路由 | 澄清是否合理 | 稳定性（超时/报错） | 备注                                                                                                                        |
| :--: | :-- | :------- | :------------- | :------------------------ | :--------: | :----------- | :------------------ | :-------------------------------------------------------------------------------------------------------------------------- |
|  1   | R+1 | 应召回   | 仅车牌不提日期 | `generate_vehicle_report` |   ✅ 否    | N/A          | 正常                | 命中，正确                                                                                                                  |
|  2   | R+2 | 应召回   | 口语化表述     | `generate_vehicle_report` |   ✅ 否    | N/A          | 正常                | 命中，正确                                                                                                                  |
|  3   | R+3 | 应召回   | 不规范写法     | `generate_vehicle_report` |   ✅ 否    | N/A          | 正常                | 命中，正确（"广东"→"粤"成功解析）                                                                                           |
|  4   | R+4 | 应召回   | 多轮澄清       | `consult_vehicle_expert`  |   ❌ 是    | Q1澄清合理   | 正常                | **Q2 失败**：应为 `generate_vehicle_report`，实际 `consult_vehicle_expert`；assistant 重复了 Q1 的澄清文本，Q2 未被正确处理 |
|  5   | R+5 | 应召回   | 中英 vehicle   | `generate_vehicle_report` |   ✅ 否    | N/A          | 正常                | 命中，正确                                                                                                                  |
|  6   | R−1 | 不应召回 | 驾驶员易混     | `generate_driver_report`  |   ✅ 否    | N/A          | 正常                | 正确路由至驾驶员管线                                                                                                        |
|  7   | R−2 | 不应召回 | 零碎信息       | `consult_omni`            |   ✅ 否    | N/A          | 正常                | 正确，未生成车辆报告                                                                                                        |
|  8   | R−3 | 不应召回 | 仅确认数据     | `consult_vehicle_expert`  |   ✅ 否    | N/A          | 正常                | 正确，非 `generate_vehicle_report`                                                                                          |
|  9   | R−4 | 不应召回 | 两车并排       | `consult_vehicle_expert`  |   ✅ 否    | N/A          | 正常                | 正确，未生成车辆报告；系统查询了基础档案而非出正式报告                                                                      |
|  10  | R−5 | 不应召回 | 无车牌泛问     | `consult_omni`            |   ✅ 否    | N/A          | 正常                | 正确，通用咨询未生成车辆报告                                                                                                |

说明：**「是否误路由」** — 以 §4.1 为准：R+ 若 `metadata.tool` ≠ `generate_vehicle_report` 记失败；R− 若 `metadata.tool` = `generate_vehicle_report` 记失败。

---

## 7. 执行记录（正文）

（本轮实测完成后，按 R+1～R−5 顺序粘贴：用户原文、助手最终答复摘要、**该轮 `metadata.tool` 取值**、与 §4.1 是否一致。）

### R+1 — 应召回（仅车牌不提日期）

- 用户原文：「车牌 粤A02013D，管理层要看车辆安全风险画像，按管理视角整一份完整输出。」
- `metadata.tool`：`generate_vehicle_report`
- 与 §4.1 一致：✅ 命中

### R+2 — 应召回（口语化表述）

- 用户原文：「帮搞一份 粤A02650D 的安全方面整车总结，不要碎片化问答那种。」
- `metadata.tool`：`generate_vehicle_report`
- 与 §4.1 一致：✅ 命中

### R+3 — 应召回（不规范写法）

- 用户原文：「车牌 广东A02650D（广东即粤），要车辆安全总结报告，正式版。」
- `metadata.tool`：`generate_vehicle_report`
- 与 §4.1 一致：✅ 命中（"广东"→"粤"成功解析）

### R+4 — 应召回（多轮澄清）

- Q1：「我要车辆层面的安全总结，模块给全。」→ 系统正确追问车牌号
- Q2：「车牌号是 粤A02650D。」→ `metadata.tool`：`consult_vehicle_expert`
- 与 §4.1 不一致：❌ 失败；Q2 未被正确处理，assistant 重复了 Q1 的澄清文本，应命中 `generate_vehicle_report`

### R+5 — 应召回（中英 vehicle）

- 用户原文：「Vehicle safety risk profile (management version) for plate 粤A02013D; full formal output.」
- `metadata.tool`：`generate_vehicle_report`
- 与 §4.1 一致：✅ 命中

### R−1 — 不应召回（驾驶员易混）

- 用户原文：「出一份驾驶员安全画像，师傅 任宇邦，不要车辆维度。」
- `metadata.tool`：`generate_driver_report`
- 与 §4.1 一致：✅ 正确（不是 vehicle report）

### R−2 — 不应召回（零碎信息）

- 用户原文：「不用出长篇报告，查一下 粤A02650D 的所属车队、年检到期日就行。」
- `metadata.tool`：`consult_omni`
- 与 §4.1 一致：✅ 正确（不是 vehicle report）

### R−3 — 不应召回（仅确认数据）

- 用户原文：「别生成报告。告诉我 粤A02013D 有没有车辆安全画像数据就好。」
- `metadata.tool`：`consult_vehicle_expert`
- 与 §4.1 一致：✅ 正确（非 `generate_vehicle_report`）

### R−4 — 不应召回（两车并排）

- 用户原文：「粤A02650D 和 粤A02013D 两台车一起看安全概况，不给单车的长篇报告。」
- `metadata.tool`：`consult_vehicle_expert`
- 与 §4.1 一致：✅ 正确（查询基础档案而非出正式报告）

### R−5 — 不应召回（无车牌泛问）

- 用户原文：「公交车辆日常安全管理一般需要关注哪些方面？」
- `metadata.tool`：`consult_omni`
- 与 §4.1 一致：✅ 正确（通用咨询未生成车辆报告）

---

**召回率统计**：

- R+ 命中率：4/5 = 80%（R+1~R+3、R+5 命中；R+4 Q2 失败）
- R− 正确率：5/5 = 100%（R−1~R−5 全部正确拒绝车辆报告管线）
- 误触发数：0
