# MCP Tool 文档模板（超精简版）

> **用途**：单 tool 一份，供接入与验收。  
> **接入基线**（HTTP endpoint、联调顺序、判错与 `content[].text`）：见《[mcp_tools_交接模板.md](./mcp_tools_交接模板.md)》文首「BUS 当前接入基线」。

---

## 1. 元信息与 tool 标识

| 项目 | 内容 |
|---|---|
| MCP Server 名称 |  |
| MCP 协议版本（服务端实际支持） | 例：`2025-03-26` |
| `name`（`tools/call` 使用的标识） |  |
| Tool 中文名称（给人看） |  |
| 文档版本 / 修订日期 |  |
| 作者 / 对接人 |  |

---

## 2. Tool description

**注册到 MCP 的 `description`（用途、触发条件、边界与限制）：**

```text

```

---

## 3. inputSchema（完整 JSON Schema）

> 须与 `tools/list` 中该 tool 的 `inputSchema` **一致**；JSON Schema 2020-12，根为 `object`。

```json

```

---

## 4. 可选 MCP 扩展字段（有则填，无则整节写「均未使用」）

### 4.1 `title`（可选）

| `title` 值 | 未注册可填「未注册」 |
|---|---|
|  |  |

### 4.2 `outputSchema`（可选，用于校验 `structuredContent` 时）

```json

```

**与 `structuredContent` 的对应关系（一句话）：**  

### 4.3 `annotations`（可选）

| 字段 | 值 |
|---|---|
| `readOnlyHint` |  |
| `destructiveHint` |  |
| `idempotentHint` |  |
| `openWorldHint` |  |

> `annotations` 仅为提示，不能替代鉴权。

---

## 5. 参数说明

> 若 **§3** 已含完整字段说明，本表可只补 **联合校验** 与 **非法/缺失时行为**。

| 参数名 | 必填 | 含义与约束 | 缺失或非法时 |
|---|---|---|---|
|  |  |  |  |

**多字段联合校验（如有）：**  

---

## 6. `tools/call` 返回与错误示例

> 均为 JSON-RPC 成功响应里的 `result`（`CallToolResult`），**协议层错误**见 **6.4**（无 `result`）。

### 6.1 成功（有数据）

```json

```

### 6.2 成功但空结果

> 须与 **异常** 区分；`isError` 为 false 或不强调为错误。

```json

```

### 6.3 Tool Execution Error（`isError: true`）

```json

```

### 6.4 协议错误（JSON-RPC `error`，非 `CallToolResult`）

```json

```

---

## 7. 业务错误码（选填）

> 无统一枚举时可写「见 §6.3 文案 / 上游 HTTP code」，并简述映射。

| 业务错误码 | 含义 | 可重试 | 处理要点 |
|---|---|---|---|
|  |  |  |  |

---

## 8. 数据来源与业务口径（精简）

| 项目 | 内容 |
|---|---|
| 数据来源（系统/接口） |  |
| 时间 / 统计 / 排序口径 |  |
| 特别说明（延迟、脏数据、口径变更，无则写「无」） |  |

---

## 9. 可观测与排障（精简）

**是否透传 `trace_id` / `request_id`：**  

**关键日志字段（表内可只列与本 tool 相关的）：**

| 字段 | 必有 | 说明 |
|---|---|---|
|  |  |  |

**常见现象 → 排查：**（一两段：如空数据、超时、认证失败）

---

## 10. 写操作安全（如适用）

> **只读 tool** 在下方写一句即可，并删表。

**本 tool：** ☐ 只读，无写操作 ☐ 有写副作用（请填表）

| 幂等性 | 可重试错误 | 幂等键/流水号 | 部分成功与补偿 |
|---|---|---|---|
|  |  |  |  |

---

## 11. 验收用例（至少 各 3 条）

### 用例 1：正常

- **输入：**  
- **预期：**  
- **标准：**  

### 用例 2：边界

- **输入：**  
- **预期：**  
- **标准：**  

### 用例 3：异常

- **输入：**  
- **预期：**  
- **标准：**  

---

## 填写自检（本模板 1～11）

- [ ] 1 元信息 + `name`  
- [ ] 2 `description`  
- [ ] 3 `inputSchema`  
- [ ] 4 可选 title / outputSchema / annotations（或注明均未使用）  
- [ ] 5 参数说明（或与 schema 对照后的补充）  
- [ ] 6 四类示例齐全  
- [ ] 7 业务错误码（或选填说明）  
- [ ] 8 数据与口径  
- [ ] 9 可观测与排障  
- [ ] 10 写操作（只读一句话或填表）  
- [ ] 11 验收用例 ≥3  